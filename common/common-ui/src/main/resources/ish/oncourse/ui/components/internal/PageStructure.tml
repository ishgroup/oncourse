<!DOCTYPE html>
<html xmlns="http://www.w3.org/1999/xhtml" xmlns:t="http://tapestry.apache.org/schema/tapestry_5_1_0.xsd" xmlns:p="tapestry:parameter" lang="en">
	<t:remove>
		Purpose: html, head and body tags for the entire page. This is called for
		every single full page which is rendered by onCourse.
		Override: No. This component should not normally be overridden
	</t:remove>
	<t:if test="wrapped">
		<head>
			<meta http-equiv="Cache-control" content="cacheMeta"/>
			<span t:type="ui/pagehead" pageName="pageName" webNode="webNode"
				  canonicalLinkPath="canonicalLinkPath"
				  metaDescription="metaDescription"/>
		</head>
		<body t:type="any" class="prop:agentAwareBodyClass" id="prop:bodyId">
            <span t:type="ui/googletagmanager" eventName="eventName" cart="cart"/>

            <div  t:type="ui/BodyStructure" webNodeType="webNodeType">
				<t:body/>
			</div>
		</body>
		<p:else>
			<t:body/>
		</p:else>
	</t:if>
</html>