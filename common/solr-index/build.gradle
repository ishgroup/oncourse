apply from: "$rootDir/gradle/functions.gradle"

apply plugin: 'groovy'
apply plugin: 'java'


sourceSets.main.java.srcDirs = []
sourceSets.main.groovy.srcDir 'src/main/java'

sourceSets.test.java.srcDirs = []
sourceSets.test.groovy.srcDir 'src/test/java'

compileGroovy.options.encoding = 'UTF-8'
compileTestGroovy.options.encoding = 'UTF-8'

sourceSets {
    schema {
        resources {
            srcDir 'src/main/resources/solr'
        }
    }
}

dependencies {

    testCompile 'rg.hamcrest:hamcrest-all:2.2'
    testCompile 'org.apache.lucene:lucene-analyzers-common:7.1.0'
    testCompile 'org.apache.lucene:lucene-test-framework:7.1.0'
    compile project(':common:cayenne-model')
    compile project(':common:common-functions')
    compile project(':common:job-scheduler')
    compile group: 'org.apache.cayenne', name: 'cayenne-commitlog', version: cayenneVersion
    compile group: 'org.apache.solr', name: 'solr-solrj', version: solrVersion
    compile group: 'com.fasterxml.jackson.core', name: 'jackson-databind', version: '2.9.2'
    compile group: 'io.reactivex.rxjava2', name: 'rxjava', version: '2.1.6'

    testCompile group: 'org.apache.solr', name: 'solr-dataimporthandler', version: solrVersion
    testCompile project(':common:test-functions')
    testCompile group: 'org.mariadb.jdbc', name: 'mariadb-java-client', version: mariadbVersion
    testCompile group: 'com.google.code.gson', name: 'gson', version: '2.8.4'

    testCompile 'com.vividsolutions:jts:1.13'
    testCompile("org.apache.solr:solr-test-framework:${solrVersion}") {
        exclude group: 'org.apache.hadoop'
    }
}

test {
    forkEvery = 1 //run every test in separate jvm
    initTestDBConfig(project, test)
}
