apply plugin: 'java'

dependencies {
    testCompile 'com.intuit.karate:karate-junit4:0.9.0.RC4'
    testCompile 'com.intuit.karate:karate-jersey:0.9.0.RC4'
    testCompile 'com.intuit.karate:karate-core:0.9.0.RC4'
    testCompile 'org.glassfish.jersey.connectors:jersey-jetty-connector:2.26'
    testCompile 'org.eclipse.jetty.http2:http2-http-client-transport:${jettyVersion}'
    testCompile 'org.eclipse.jetty:jetty-client:${jettyVersion}'
}

test {
    dependsOn ":server:startDaemon"
    // pull cucumber options into the cucumber jvm
    systemProperty "cucumber.options", System.properties.getProperty("cucumber.options")
    // pull karate options into the jvm
    systemProperty "karate.env", System.properties.getProperty("karate.env")
    // ensure tests are always run
    outputs.upToDateWhen { false }
}