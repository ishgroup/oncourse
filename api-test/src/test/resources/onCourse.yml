db:
  uri: '${dbUrl}'

license:
  college_key: mamoth
  access_control: true
  custom_scripts: true

smtp:
  host: 127.0.0.1
  port: 11111
  mode: mock

document:
  accessKeyId: ${document.accessKeyId}
  accessSecretKey: ${document.accessSecretKey}
  bucket: ${document.bucket}
  region: ${document.region}
  endpoint: ${document.endpoint}

archives:
  accessKeyId: ${document.accessKeyId}
  accessSecretKey: ${document.accessSecretKey}
  bucket: ${document.bucket}
  region: ${document.region}
  endpoint: ${document.endpoint}
