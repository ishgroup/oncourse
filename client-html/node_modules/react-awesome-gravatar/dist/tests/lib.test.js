"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var lib_1 = require("../src/lib");
describe('getGravatarUrl', function () {
    var email = 'example@example.com';
    var baseExpectedUrl = 'https://www.gravatar.com/avatar/23463b99b62a72f26ed677cc556c44e8?';
    it('Returns the gravatar url', function () {
        expect(lib_1.getGravatarUrl(email)).toBe(baseExpectedUrl);
    });
    it('Returns the gravatar url with the correct option as a query string param', function () {
        var options = {
            size: 150,
        };
        expect(lib_1.getGravatarUrl(email, options)).toBe(baseExpectedUrl + "size=150");
    });
    it('Returns the gravatar url with multiple options are passed', function () {
        var options = {
            rating: 'x',
            size: 150,
        };
        expect(lib_1.getGravatarUrl(email, options)).toBe(baseExpectedUrl + "rating=x&size=150");
    });
    it('Returns the gravatar url with defaultUrl as the default query param', function () {
        var options = {
            default: 'monsterid',
            defaultUrl: 'https://example.com/superimage.png',
        };
        expect(lib_1.getGravatarUrl(email, options))
            .toBe(baseExpectedUrl + "default=https%3A%2F%2Fexample.com%2Fsuperimage.png");
    });
});
