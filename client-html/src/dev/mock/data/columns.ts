import { ColumnWidth } from "@api/model";

export function mockColumnsSettings(): ColumnWidth {
  return {
    preferenceLeftColumnWidth: 200,
    securityLeftColumnWidth: 200,
    tagLeftColumnWidth: 200
  };
}
