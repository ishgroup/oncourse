/*
 * Copyright ish group pty ltd. All rights reserved. https://www.ish.com.au
 * No copying or use of this code is allowed without permission in writing from ish.
 */

// import MenuItem from "@material-ui/core/MenuItem";
// import * as React from "react";
// import { connect } from "react-redux";
// import { Dispatch } from "redux";
// import { runScript } from "../actions";

// const ExecuteScriptMenuItem = props => {
//   const { selection, menuItemClass, executeScript } = props;
//
//   return (
//     <MenuItem disabled={selection.length !== 1} className={menuItemClass} onClick={() => executeScript(selection[0])}>
//       Execute Script now
//     </MenuItem>
//   );
// };

// const mapDispatchToProps = (dispatch: Dispatch<any>) => ({
  // executeScript: (id: number) => {
  //   dispatch(runScript(id));
  // }
// });

// export default connect<any, any, any>(null, null)(ExecuteScriptMenuItem);
