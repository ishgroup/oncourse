<!--The page deployed under https://secure-payment.oncourse.net.au/services/s/integrations/xero/auth URL
    ~ in case of moving this page to another address we need register new URL in xero
    ~ https://developer.xero.com/myapps
    ~ We also need adjust angel end to use different redirect page address 
-->
<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <script>
        const params = new URLSearchParams(decodeURIComponent(window.location.search));
        window.open(params.get("state") + "?code=" + params.get("code"), "_self");
    </script>
</head>
<body>
</body>
</html>