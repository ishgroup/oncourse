put:
  operationId: checkPassword
  description: Get Authenticationd
  tags:
    - Authentication
  parameters:
    - name: password
      in: path
      type: string
      required: true
  responses:
    '200':
      description: Success response.
      schema:
        $ref: './def/entity/login/pass/PasswordComplexity.yaml'
