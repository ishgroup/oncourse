get:
  summary: Get user usi required
  operationId: getUsiRequired
  tags:
    - User
  parameters:
    - name: id
      in: path
      required: true
      type: number
  responses:
    '200':
      description: Success response.
      schema:
        type: boolean
    '400':
      description: Bad request
      schema:
        $ref: './def/entity/ErrorResponse.yaml'
    default:
      description: Unexpected error
