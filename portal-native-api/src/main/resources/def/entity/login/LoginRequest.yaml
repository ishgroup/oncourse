type: object
properties:
  ssOProvider:
    description: SSO service provider
    $ref: '../../enum/SSOproviders.yaml'
  ssOToken:
    type: string
    description: one time SSO token that is needed to complite OAuth2 process
  email:
    type: string
    description: User's email
  password:
    type: string
    description: User's password
  codeVerifier:
    type: string
    description: code verified, need to check code challeng
  

