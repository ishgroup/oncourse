type: object
properties:
  user:
    description: User entity
    $ref: '../User.yaml'
  token:
    type: string
    description: session token 
  isNew:
    type: boolean
    description: is newly added user

