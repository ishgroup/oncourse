type: object
properties:
  user:
    description: User entity
    $ref: '../User.yaml'
  token:
    type: string
    description: session token 
  vefiryEmail:
    type: boolean
    description: verify email before proceed to portal app

