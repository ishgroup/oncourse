result {
	message "Course published to web has no web enabled tag."
	severity 99
}

result {
	message "Tag published to web has no web enabled course."
	severity 99
}


display list of tag names in this window. A tag published to the web without a course is a web enabled tag from a tag group attached to a course entity type.
