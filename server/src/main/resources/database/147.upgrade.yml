databaseChangeLog:
  - changeSet:
      id: 147-1
      author: dmitriy
      validCheckSum: ANY
      preConditions:
        - onFail: MARK_RAN
        - not:
          - sqlCheck:
              expectedResult: 0
              sql: 'select count(*) from Import where enabled is null'
      changes:
        - sql: 'UPDATE Import i SET i.enabled = 2 WHERE i.enabled IS NULL'
