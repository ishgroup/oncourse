type: object
properties:
  messageId:
    type: integer
    format: int64
  createdOn:
    type: string
    format: date-time
  sentOn:
    type: string
    format: date-time
  subject:
    type: string
  creatorKey:
    type: string
  status:
    $ref: '../enum/MessageStatus.yaml'
  type:
    $ref: '../enum/MessageType.yaml'