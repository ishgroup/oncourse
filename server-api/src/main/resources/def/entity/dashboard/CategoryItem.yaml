type: object
description: Category Item
properties:
  url:
    description: url link to open corresponded frame, need to open old swing windows 
    required: false
    type: string
  category:
    $ref: './Category.yaml'
    description: category enum item
    required: true
  favorite:
    type: boolean
    description: category item in favorite categories list
    required: true