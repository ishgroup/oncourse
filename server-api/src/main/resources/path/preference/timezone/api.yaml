get:
  operationId: getTimezones
  description: get available time zones
  tags:
  - Preference
  responses:
    '200':
      description: Success response.
      schema:
        type: array
        items:
          type: string
    default:
      description: Unexpected error
  x-permission:
    keyCode: UNDEFINED    