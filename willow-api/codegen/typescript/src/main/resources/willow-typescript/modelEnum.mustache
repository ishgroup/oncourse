{{#description}}
    /**
    * {{{description}}}
    */
{{/description}}
import {createStringEnum} from "common/utils/EnumUtils";

const Enum = createStringEnum([
  {{#allowableValues}}
  {{#enumVars}}
      {{{value}}}{{^-last}},{{/-last}}
  {{/enumVars}}
  {{/allowableValues}}
]);

export type {{classname}} = keyof typeof Enum;
