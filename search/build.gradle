apply plugin: 'distribution'

distributions {
    main {
        contents {
            from { project(':common:solr-index').sourceSets.schema.resources }
        }
    }
}

publishing {
    publications {
        executables(MavenPublication) {
            artifact(distZip) {
                classifier "executable"
                extension "zip"
            }
        }
    }
}