<li class="${tagItemClass}">
   	<a href="${tagLink}">${tag.name}</a>
   	<t:if test="showDetails">
		<div class="taggroup_detail courses-list-detail">
			${tag.detail}
		</div>
	</t:if>
	<ul class="courses-list-sub2">
   		<t:body/>
   	</ul>
</li>