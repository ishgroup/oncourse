<t:container xmlns:t="http://tapestry.apache.org/schema/tapestry_5_1_0.xsd" xmlns:p="tapestry:parameter">
	<div id="class-description-${session.id}" class="class-description-class ${markedClass}">
		<table class="class-desc-section">
			<tr>
				<td><object data="/portal/img/svg_icons/DIU_RAW0397.svg" type="image/svg+xml"/></td>
				<td>
				<t:if test="future">
					Mark this of <strong>Date of course</strong>
					<p:else>
						<t:if test="marked">
							Marked roll for 
							<p:else>
								You are currently marking 
							</p:else>
						</t:if>
						<strong>${sessionDate}</strong>
					</p:else>
				</t:if>
				</td>
			</tr>
			<t:if test="session.publicNotes">
				<tr>
					<td><object class="session-icon" data="/portal/img/svg_icons/DIU_RAW1512.svg" type="image/svg+xml"/></td>
					<td>${session.publicNotes}</td>
				</tr>
			</t:if>
			<t:if test="session.privateNotes">
			<tr>
				<td></td>
				<td><strong>Private notes</strong>${session.privateNotes}</td>
			</tr>
			</t:if>
		</table>
	</div>
</t:container>