<html t:type="pagestructure" title="literal:Resources"
	  xmlns:t="http://tapestry.apache.org/schema/tapestry_5_1_0.xsd" activeMenu="literal:resources">
    <t:if test="hasAnyResources()">
	<div class="page-header clearfix">
		<h1><a href="#">Resources</a></h1>
	</div>
	<ul class="list-unstyled resources-content">

		<t:if test="sudentAndTutorsMaterials">
			<li>
				<h4><a href="#">Students information</a></h4>
				<ul class="list-unstyled resources-list">
					<t:loop source="sudentAndTutorsMaterials" value="sudentAndTutorsMaterial">
						<li class="${sudentAndTutorsMaterial.type}">
							<img src="${contextPath}/img/ico-${sudentAndTutorsMaterial.type}.png"
								 alt=""/>
							<a href="${getMaterialUrl(sudentAndTutorsMaterial)}" target="_blank">
								${sudentAndTutorsMaterial.name}<t:if test="needExtension()">.${sudentAndTutorsMaterial.type}</t:if>
							</a>
							${getSize(sudentAndTutorsMaterial)}.
							<t:if test="isNew(sudentAndTutorsMaterial.modified)">
								<span class="label label-primary">NEW</span>
							</t:if>
						</li>
					</t:loop>
				</ul>
			</li>
		</t:if>

        <t:loop source="courseClasses" value="courseClass">

          <t:if test="${hasResources(courseClass)}">
              <h6>
                  <t:pagelink page="class" context="courseClass.id">${courseClass.course.name}</t:pagelink>
                  <small> (${courseClass.uniqueIdentifier})</small>
              </h6>
              <small class="text-muted">${getDate(courseClass)}</small>
              <ul class="list-unstyled">
                <span t:type="courseclass/classresources" courseClass="courseClass"/>
              </ul>
          </t:if>

        </t:loop>
	</ul>
</t:if>
    <t:unless test="hasAnyResources()">
        <h3>No resources available.</h3>
    </t:unless>
</html>