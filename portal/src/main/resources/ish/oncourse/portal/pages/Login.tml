<html t:type="pagestructure" title="literal:Login page"
	  xmlns:t="http://tapestry.apache.org/schema/tapestry_5_1_0.xsd" xmlns:p="tapestry:parameter">
<div class="col-xs-12 col-sm-12 col-lg-12">
	<div class="login-form well">
		<t:form t:id="loginForm" method="post" clientValidation="literal:NONE">
			<t:errors banner="literal:" class="literal:validation alert alert-block alert-danger"/>
			<fieldset><h2 class="form-title">Log In</h2>

				<div class="login-option clearfix form-group">
					<div class="slider-frame"><label class="slider-button ${companyCheckStyle.companyOn}" for="chk_company">${companyCheckStyle.label}</label>
						<t:checkbox t:id="iscompany" value="iscompany" clientId="chk_company" name="chk_company"/></div>
				</div>

				<div class="company-login" style="${companyCheckStyle.companyDisplay}">
					<div class="form-group ${getErrorClass('companyName')}"><t:textField t:id="companyName" class="form-control" placeholder="Company Name"/></div>
					<span t:type="HintComponent" validationMessage="${getValidationMessage('companyName')}"/>
				</div>

				<div class="user-login"  style="${companyCheckStyle.studentDisplay}">
					<div class="form-group  ${getErrorClass('firstName')}">
						<t:textField t:id="firstName" class="form-control" placeholder="First name"/>
						<span t:type="HintComponent" validationMessage="${getValidationMessage('firstName')}"/>
					</div>

					<div class="form-group ${getErrorClass('lastName')}">
						<t:textField t:id="lastName" class="form-control " placeholder="Last name"/>
						<span t:type="HintComponent" validationMessage="${getValidationMessage('lastName')}"/>
					</div>
				</div>

				<div class="form-group ${getErrorClass('email')}">
					<t:textField t:id="email" class="form-control" placeholder="Email"/>
					<span t:type="HintComponent" validationMessage="${getValidationMessage('email')}"/>
				</div>
				<t:if test="!firstTimeLogin">
					<div class="form-group ${getErrorClass('password')}">
						<t:passwordField t:id="password" class="form-control" placeholder="Password"/>
						<span t:type="HintComponent" validationMessage="${getValidationMessage('password')}"/>
					</div>
				</t:if>

				<div class="row">
					<div class="col-lg-12">
						<t:if test="firstTimeLogin">
							<div class="text-right">	
								<t:submit t:id="createAccount" class="btn-login btn btn-primary" value="Create account"/>
							</div>
						</t:if>
						<t:unless test="firstTimeLogin">
							<!--Duplicate main submit (login) in first position, make it invisible.  It is necessary to catch 'Enter'
							 key press action, since it has affect only on first 'submit' input-->
							<input value="Submit" id="signIn" class="hidden" data-submit-mode="normal" name="signIn" type="submit"/>
							
							<div id="forgotPassword" class="forget-pwd" >
								<t:submit t:id="forgotPassword" class="btn-login btn btn-primary" value="Forgot Password?"/>
							</div>
							<div class="text-right">
								<t:submit t:id="signIn" class="btn-login btn btn-primary" value="Submit"/>
							</div>
						</t:unless>
					</div>
				</div>
			</fieldset>
		</t:form>
		<div class="alert alert-info">
		<t:if test="firstTimeLogin">
			<p>It looks like this is your first visit to skillsOnCourse. Create an account to login and manage your classes, view your timetable and much more.</p>
			<p:else><p>Login to skillsOnCourse if you are a tutor or a student. Manage your
				classes, view your timetable and much more.</p>
			</p:else>
		</t:if>
		
		</div>
	</div>
</div>
</html>

