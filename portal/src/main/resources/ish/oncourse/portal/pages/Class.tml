<html t:type="pagestructure" title="literal:Class"
      xmlns:t="http://tapestry.apache.org/schema/tapestry_5_1_0.xsd" activeMenu="literal:m_classes">



        <t:if test="${needApprove()}">
            <p class="info-text fixed">This course requires approval <a href="${request.contextPath}/tutor/classapproval/${courseClass.id}" class="btn btn-primary btn-xs">Review now</a></p>
        </t:if>
        <div class="page-header zero-padding clearfix">
                <ul class="nav nav-tabs header-tabs">
                    <span t:type="courseclass/classTabs" courseClass="courseClass"/>
                </ul>
                <h3>
                    <t:if test="url">
                        <a href="url" target="_blank">${courseClass.course.name}</a>
                    </t:if>
                    <t:unless test="url">
                        <a href="#">${courseClass.course.name}</a>
                    </t:unless>

                    <span class="info">
                        <small>  (${courseClass.uniqueIdentifier})</small>
                        <t:if test="${needApprove()}">
                            <a href="${request.contextPath}/tutor/classapproval/${courseClass.id}" class="label label-primary">Review now</a>
                        </t:if>
                    </span>
                </h3>
        </div>
        <div class="tab-content">
            <span t:type="courseclass/classDetails" courseClass="courseClass"></span>
            <span t:type="courseclass/classLocation" courseClass="courseClass" ></span>
            <t:if test="hasResources()">
                <span t:type="courseclass/classResources" courseClass="courseClass"></span>
            </t:if>
            <t:if test="hasResults()">
                <span t:type="courseclass/classResults" courseClass="courseClass"></span>
            </t:if>
        </div>

</html>