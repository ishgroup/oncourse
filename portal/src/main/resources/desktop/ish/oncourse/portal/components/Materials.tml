<t:container xmlns:t="http://tapestry.apache.org/schema/tapestry_5_1_0.xsd">

	<t:loop source="materials" value="material">	
		<div class="${material.type}">
			<img src="/img/ico-${material.type}.png" alt=""/>
			<a href="/asset/binarydata?id=${material.id}">${material.name}<t:if test="material.type.empty" negate="true">.${material.type}</t:if></a> ${material.size} kb.
		</div>
	</t:loop>

</t:container>