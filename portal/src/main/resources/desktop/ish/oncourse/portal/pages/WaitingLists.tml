<html t:type="pagestructure" title="literal: Waiting Lists"
      xmlns:t="http://tapestry.apache.org/schema/tapestry_5_1_0.xsd" activeMenu="literal:m_waitinglists" xmlns:p="tapestry:parameter">
    <div class="waiting_list">
		<h1>Waiting Lists</h1>
        <t:if test="waitingLists">
        	<div>Your are on the waiting list for the following courses.</div>
            <t:loop  source="waitingLists" value="waitingList">
                <div t:type="WaitingListInfo" waitingList="waitingList"/>
            </t:loop>
			<p:else>
				<div>You are not currently on any waiting lists.</div>
			</p:else>
        </t:if>
    </div>

</html>